[build]
  builder = "nixpacks"
  buildCommand = "pip install -r mobile/requirements.txt"

[deploy]
  startCommand = "gunicorn --chdir mobile api_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
  restartPolicyType = "on_failure"
